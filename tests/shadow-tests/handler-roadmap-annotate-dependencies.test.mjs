import { test } from 'node:test';
import assert from 'node:assert/strict';

// STUB — handler-roadmap-annotate-dependencies: production code not yet written (Wave 0 marker)
//
// CASE 1: handler creates blocks dep links between phases
// CASE 2: handler returns { data: {...} } with backend: 'beads'
// CASE 3: handler errors gracefully on unknown phase-id

test('STUB — handler-roadmap-annotate-dependencies: production code not yet written', () => {
  assert.fail('handler-roadmap-annotate-dependencies stub — Wave 0 marker');
});
