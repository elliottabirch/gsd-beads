# Spike Manifest

## Idea

Validate that the **gsd-beads** integration design (see
`.planning/notes/beads-gsd-architecture.md`) actually works on this developer's
machine before committing to the full Phase 2 build. Beads becomes the source
of truth for workflow state; state-bearing markdown becomes a regenerated
read-only view; hooks deterministically enforce the write path; GSD core is
unmodified. Each assumption in `notes/spike-validation-plan.md` gets exercised.

## Requirements

Decisions that emerged from spiking. Non-negotiable for the real build.
Updated as spikes progress.

- **Use the structured `permissionDecision` JSON output (exit-0 path), not exit-2 + stderr.** The reason string reaches the agent and the path is future-compatible. (Spike 001)
- **`PreToolUse` matcher must be `Edit|Write`; path filtering happens inside the hook script** (bash `case` glob on `tool_input.file_path`). The matcher field cannot filter on file path. (Spike 001)
- **`PostToolUse` Bash filtering uses the `if: "Bash(bd *)"` field on the handler** — not the matcher. (Spike 001)
- **The install script must guarantee `jq` and `bd` are on `PATH`** before activating the hook fragment, or fail with a clear error. (Spike 001)
- **The install script must deep-merge `settings.fragment.json` into `~/.claude/settings.json`** with array deduplication on `(matcher, command)`; no native tool exists. (Spike 001)
- **State-bearing path set is locked:** `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/todos/**`, `.planning/seeds/**`. Both absolute and relative shapes must be recognized. Narrative MD (`PLAN.md`, `RESEARCH.md`, `AI-SPEC.md`, `UI-SPEC.md`, `DISCUSSION-LOG.md`) and source code are unaffected. (Spike 001)
- **~~`bd init --stealth --non-interactive` is the canonical install command~~** — REVERSED in Spike 003. The canonical install is `bd init --non-interactive --skip-agents`, **NOT** `--stealth`. Beads is designed to be git-tracked: `.beads/issues.jsonl` (workflow state SoT) is auto-exported and auto-staged after every write, while `.beads/embeddeddolt/` (binary cache, rebuildable from JSONL) is gitignored. Stealth defeats this entire design — losing git audit history of workflow changes, multi-machine sync via `git push/pull`, new-clone bootstrap via `bd init --from-jsonl`, and the value of bd's bundled `post-merge` hook. (Spike 002 → revised in Spike 003)
- **gsd-beads LAYERS on top of `bd setup claude`, not replaces it.** Install order: `bd init` (non-stealth) → `bd setup claude` (installs SessionStart/PreCompact + bd's own CLAUDE.md sentinel block pointing at `bd prime`) → `bd hooks install` → gsd-beads install (adds PreToolUse(Edit|Write) blocker + PostToolUse(Bash, "Bash(bd *)") sync + seeds `bd remember --key gsd-beads:*` memories). The hook sets are orthogonal; persistent gsd-beads instructions go in bd memories, NOT in a CLAUDE.md addendum (memories are auto-surfaced by `bd prime`). (Spike 002, corrected during review)
- **gsd-beads is distributed as a bd custom recipe** via `bd setup --add gsd-beads <path>`. Reuses beads' install/update/remove machinery; supersedes "Path A: symlinks managed by windows-dev-setup" in `notes/beads-gsd-architecture.md`. (Spike 002)
- **Type strategy: all-epic + labels.** Both requirement-level and phase-level beads use `type=epic`. Distinguish via labels: `bd label add <id> gsd:requirement` for top-level, `bd label add <id> gsd:phase` for middle-level, no label needed for `task`-typed leaves. ROADMAP regen filter: `bd list --type=epic -l gsd:requirement --json` for requirements, `bd list --type=epic -l gsd:phase --json` for phases. **Rationale:** 3× faster cascade vs custom types (0.86s vs 2.7s on 7-issue fixture); leverages bd's built-in `bd epic close-eligible`; future bd cascade improvements apply automatically; agents already know `epic` from `bd prime`. (Spike 002)
- **Cascade is a 5-line loop on `bd epic close-eligible`** (see `cascade-loop.sh`). Invoked from `bd-sync.sh` after every `bd ` PostToolUse call. Idempotent — safe to call repeatedly. (Spike 002)
- **Always use `bd link --type parent-child`** (NOT `bd dep add`'s default `blocks`) for the requirement→phase→task hierarchy. (Spike 002)
- **Use `bd label propagate`** to inherit `gsd:phase` / `gsd:requirement` labels down through children when restructuring — durability mitigation. (Spike 002)
- **Run `bd hooks install` as part of gsd-beads install.** Adds 5 sentinel-merged git hook shims (pre-commit, post-merge, pre-push, post-checkout, prepare-commit-msg) with 300s timeout + graceful "no bead store" fallback. The `prepare-commit-msg` agent-identity trailer is valuable for forensics on agent-driven commits. (Spike 002)
- **Seeds map to beads issues with label `gsd:seed`** (deferred status), NOT to `bd remember` memories. `bd remember` is for cross-cutting conventions and gotchas; seeds are forward-looking work items with trigger conditions. (Spike 002)
- **`bd federation` is orthogonal to cross-worktree (spike 003)** — federation is for peer-to-peer between separate Dolt DBs on different machines; `BEADS_DIR` is for one-developer multi-worktree. Not needed for single-developer MVP. (Spike 002)
- **Cross-worktree sharing: source-repo's `.beads/` is the canonical store; worktrees point at it via `BEADS_DIR=<source-repo>/.beads`.** Workflow state (`.beads/issues.jsonl`) is committed to git in the source repo; the binary embeddeddolt cache is gitignored. Worktree writes flow back to the source repo's `.beads/` via the BEADS_DIR override and produce clean line-diffs in `issues.jsonl` that git tracks. No `bd dolt push` between worktrees needed; no external shared directory required. `--shared-server` mode is documented as escape hatch for heavy concurrency, not required for MVP. (Spike 003)
- **New worktrees auto-configured via post-checkout hook.** `git worktree add` reliably fires `post-checkout` in the new worktree (verified empirically: `PWD=<new-wt>`, `flag=1` for branch checkout). gsd-beads' install adds a sentinel-marked block to `.beads/hooks/post-checkout` (alongside bd's own shim) that, on first checkout in a new worktree: persists `git config --worktree gsd-beads.dir <source-repo>/.beads` so the BEADS_DIR location is discoverable per-worktree without env-var discipline. Idempotent via marker file in the worktree's git-dir. (Spike 003)
- **Hash-based ID uniqueness is solid under concurrent load.** 125 beads created across 40 concurrent processes from 2 worktrees: 125 unique IDs, 0 collisions. Embedded Dolt serializes concurrent writes via file lock at ~3.25 writes/sec; correctness is guaranteed even though parallel speedup is not. (Spike 003)
- **Fresh-clone bootstrap = `git clone` + `bd init --from-jsonl`** with explicit `--prefix` read from `.beads/metadata.json`'s `dolt_database` field. JSONL roundtrip preserves every field that matters: IDs, statuses, parent-child trees, labels, close_reasons (including bd's `"All children completed"` cascade marker), notes, and issue_type. Without explicit `--prefix`, new post-clone IDs use the clone-directory name and diverge from pre-clone IDs — gsd-beads' bootstrap script must always set it. (Spike 004)
- **gsd-beads bootstrap script (Phase 2):** ```prefix=$(jq -r .dolt_database .beads/metadata.json 2>/dev/null || basename "$PWD"); bd init --from-jsonl --prefix "$prefix" --non-interactive --skip-agents; chmod 700 .beads```. Invoked once per fresh clone, ideally auto-triggered by gsd-beads' SessionStart hook if `.beads/embeddeddolt/` is missing. (Spike 004)
- **Concurrent writes are safe via embedded Dolt + file lock** — different-field updates from parallel processes both persist (21/21 in heavy test); same-field contention is last-writer-wins with no errors; concurrent close is idempotent. The architecture's "Dolt cell-level merge" claim is technically about server/federation modes, but the practical effect (no lost writes) holds for embedded too. (Spike 005)
- **`discovered-from` edge type is the architecture's signature feature.** When an agent uncovers missed work mid-phase, file with `bd link <new-task> <originating-task> --type discovered-from` (in addition to `--type parent-child` to its proper home). Seed via `bd remember --key gsd-beads:discovered-from "..."` at install time so agents reach for it instinctively. (Spike 005)
- **Phase 2 scope: 13 `/gsd-beads-*` substitute skills required** (the architecture doc's 8 was incomplete). Full list: `add-phase`, `add-todo`, `add-backlog`, `plant-seed`, `new-milestone`, `new-project`, `roadmapper`, `insert-phase`, `review-backlog`, `plan-milestone-gaps`, `complete-milestone`, `from-gsd2`, `check-todos`. (Spike 006)
- **Four substitutes are dual-writers** (write to BOTH ROADMAP.md AND REQUIREMENTS.md in one flow): `new-project`, `new-milestone`, `complete-milestone`, `roadmapper`. `bd-sync.sh` regen ordering must handle paired changes from one bd transaction. (Spike 006)
- **17 GSD readers must parse the regenerated views correctly.** Includes `gsd-progress`, `gsd-next`, `gsd-manager`, plus most planning-phase agents (planner, plan-checker, phase-researcher, etc.). The regen template's format contract is dictated by the union of their parse expectations. (Spike 006 → Spike 007's input)
- **`gsd-progress` and `gsd-next` are READ-ONLY**, despite the architecture doc anticipating `/gsd-beads-progress`. They work as-is on regenerated ROADMAP.md — one fewer substitute needed if the regen format is correct. (Spike 006)
- **Format contract for ROADMAP.md / REQUIREMENTS.md regen is fully documented.** The schema needs convention labels (`req-id:<id>`, `category:<slug>`, `version:v1|v2|out-of-scope`, `milestone:<id>`) plus a description-format convention for `Goal:` and `Success Criteria:` (which beads doesn't natively model). `gsd-progress` and `gsd-milestone-summary` are the most fragile readers — regen tests should focus there. (Spike 007)
- **TodoWrite carve-out:** TodoWrite (Claude Code built-in) is allowed for in-session ephemeral progress tracking. bd issues are for cross-session work items. PreToolUse hook does NOT match TodoWrite (it only matches Edit/Write). Seed via `bd remember --key gsd-beads:todowrite "..."` at install time. (Spike 008)
- **`/gsd-beads-new-project` substitute is ~3× the size of upstream `/gsd-new-project`** (~150 lines). 7 unchanged narrative steps + 3 new bd-mediated steps (requirements via `bd create + label`, phases via `bd create + label + parent-child links`, regen trigger). Same questioning UX preserved. (Spike 009)
- **Three layers needed for robust gsd-beads behavior; all required, none alone sufficient:** (1) **soft path** — bd memories under `gsd-beads:` namespace, surfaced by `bd prime` at SessionStart, prime all sessions on the gsd-beads vocabulary and rules; (2) **hard path** — PreToolUse hook deterministically denies state-bearing writes; (3) **substitute path** — 13 `/gsd-beads-*` skills are the entry points users and agents reach for. Upstream agents CANNOT be modified per REQ-02. (Spike 010)
- **`/gsd-spike-wrap-up` is safe in beads-managed projects** — it writes to `./.claude/skills/spike-findings-*/`, NOT to state-bearing paths. (Spike 011)
- **All persistent gsd-beads instructions live as `bd remember` memories**, NOT as CLAUDE.md addendum. Memories are seeded at install time under the `gsd-beads:` key namespace, surfaced automatically by `bd prime` (which runs on bd's bundled SessionStart and PreCompact hooks). Properties: searchable (`bd memories gsd-beads`), exported with the JSONL roundtrip (Spike 004), updatable via `bd forget gsd-beads:* && reseed-script` on `gsd-beads update`, no manual file editing required. **Canonical key list:** `gsd-beads:vocabulary`, `gsd-beads:state-paths`, `gsd-beads:type-strategy`, `gsd-beads:link-default`, `gsd-beads:discovered-from`, `gsd-beads:todowrite`, `gsd-beads:dolt-push`. (Cross-cutting correction during spike review — applies to spikes 002, 003, 005, 008, 009, 010)
- **`bd dolt push` is for federation/remote sync, NOT local cross-worktree sync.** Must clarify via `bd remember --key gsd-beads:dolt-push "..."` so agents don't unnecessarily invoke it after every cross-worktree change. (Spike 003)
- **Tree rendering uses `bd children <id>`, NOT `bd dep tree`.** `dep tree` walks `blocks` deps; `children` walks `parent-child`. Generated ROADMAP.md must source from `bd children` or direct JSON queries. (Spike 002)
- **`bd prime` is beads' canonical operational SSOT.** It outputs ~80 lines of dynamic workflow context, including all bd memories. gsd-beads piggybacks on it: persistent gsd-beads instructions live as `bd remember` memories under the `gsd-beads:` namespace and are surfaced automatically. No CLAUDE.md addendum needed. (Spike 002)

## Spikes

| # | Name | Type | Validates | Verdict | Tags |
|---|------|------|-----------|---------|------|
| 001 | hook-semantics | standard | `PreToolUse(Edit, ROADMAP.md)` blocks with structured error; `PostToolUse(Bash, "^bd ")` fires every time | ✓ VALIDATED | hooks, determinism, claude-code |
| 002 | beads-modeling | standard | `bd` installs; custom types `requirement`/`phase` work; parent/child auto-close cascade fires | ⚠ VALIDATED-WITH-REFINEMENT | bd, modeling, custom-types, cascade, bd-recipes |
| 003 | cross-worktree-sharing | standard | Two worktrees with shared `BEADS_DIR` see identical state; writes propagate without manual sync | ✓ VALIDATED | bd, worktrees, BEADS_DIR, embedded-dolt |
| 004 | jsonl-roundtrip | standard | `bd export` → `bd init --from-jsonl` reconstructs an equivalent graph | ⚠ VALIDATED-WITH-WORKAROUND | bd, jsonl, portability, fresh-clone-bootstrap |
| 005 | concurrent-merge | standard | Two parallel processes mutating one issue both persist; rapid cross-process creates produce zero ID collisions; `discovered-from` provenance edges work | ✓ VALIDATED | bd, concurrency, dolt, hash-ids, discovered-from, provenance |
| 006 | gsd-skill-interaction-matrix | standard | Inventory all GSD skills/agents touching state-bearing MD; classify each as BLOCKS/READS/MENTIONS under the PreToolUse hook; produce the canonical `/gsd-beads-*` substitute list | ⚠ VALIDATED-WITH-FINDINGS (13 substitutes needed, not 8) | gsd-ecosystem, hooks, skill-inventory, scope-expansion |
| 007 | reader-skill-format-contract | standard | Document the exact regex/parse expectations of every GSD skill/agent that READS state-bearing MD; build a regenerated-MD template that satisfies all of them | ⚠ VALIDATED-WITH-DESIGN-IMPLICATIONS | gsd-ecosystem, regen, format-contract, schema-extensions |
| 008 | todowrite-vs-bd-collision | standard | Resolve the conflict between Claude Code's built-in TodoWrite tool (used by GSD skills for in-session tracking) and bd's CLAUDE.md rule "do NOT use TodoWrite"; decide carve-out / hook-intercept / convention | ✓ VALIDATED (carve-out documented) | gsd-ecosystem, todowrite, claude-code, bd-rules |
| 009 | gsd-new-project-e2e | standard | Design `/gsd-beads-new-project` substitute path | ⚠ VALIDATED-WITH-DESIGN | gsd-ecosystem, e2e, onboarding |
| 010 | bd-aware-gsd-agents | standard | Probe whether upstream GSD agents can be bd-aware without forking | ⚠ VALIDATED-WITH-CONSTRAINTS (priming + hook + substitutes all needed) | gsd-ecosystem, agents, no-fork |
| 011 | gsd-spike-wrap-up-integration | standard | Verify `/gsd-spike-wrap-up` doesn't conflict with hook layer | ✓ VALIDATED | gsd-ecosystem, wrap-up, self-test |
